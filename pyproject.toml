[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "iac-sketch"
version = "0.1.0"
description = "A sketch for infrastructure as code."
authors = ["Zachary Hafen-Saavedra <z.hafen.saavedra@gmail.com>"]

[project.dependencies]
python = ">=3.8"